# Even local socket connections require a password. Ports are published on loopback only.
local all all scram-sha-256
host all ddia_admin,ddia_app all scram-sha-256
host replication ddia_replicator all scram-sha-256
